# Copy to android/keystore.properties. Do not commit the copied file.
# Use an absolute path when the key is outside this repository.
storeFile=/home/ubuntu/.android/ferry-fyi-upload.jks
storePassword=replace-with-keystore-password
keyAlias=replace-with-upload-key-alias
keyPassword=replace-with-key-password
