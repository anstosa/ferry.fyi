const path = require("path");

module.exports = {
  "config": path.resolve(`${__dirname}/sequelize.config.json`),
  "models-path": path.resolve("server", "models"),
  "migrations-path": path.resolve("server", "migrations"),
};
