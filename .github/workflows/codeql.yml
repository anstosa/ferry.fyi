name: CodeQL

on:
  workflow_dispatch:
  push:
    branches: [production]
  pull_request:
    branches: [production]
  schedule:
    - cron: "26 4 * * 1"

permissions:
  actions: read
  contents: read
  security-events: write

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    runs-on: ${{ matrix.runner }}
    timeout-minutes: ${{ matrix.timeout_minutes }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - language: actions
            build-mode: none
            runner: ubuntu-latest
            timeout_minutes: 30
          - language: javascript-typescript
            build-mode: none
            runner: ubuntu-latest
            timeout_minutes: 30
          - language: python
            build-mode: none
            runner: ubuntu-latest
            timeout_minutes: 30
          - language: java-kotlin
            build-mode: manual
            runner: ubuntu-latest
            timeout_minutes: 30
          - language: swift
            build-mode: manual
            runner: macos-latest
            # SwiftPM dependency resolution can exceed the default CodeQL job
            # window on a cold macOS runner.
            timeout_minutes: 60

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Use Node.js for native dependencies
        if: matrix.language == 'java-kotlin' || matrix.language == 'swift'
        uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          cache: yarn
          cache-dependency-path: yarn.lock

      - name: Install native dependencies
        if: matrix.language == 'java-kotlin' || matrix.language == 'swift'
        run: yarn install --frozen-lockfile

      - name: Resolve iOS Swift packages
        if: matrix.language == 'swift'
        run: >-
          xcodebuild -resolvePackageDependencies
          -project "ios/App/Ferry FYI.xcodeproj"
          -scheme "Ferry FYI"

      - name: Use Java for Android analysis
        if: matrix.language == 'java-kotlin'
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "21"

      - name: Set up Android SDK
        if: matrix.language == 'java-kotlin'
        uses: android-actions/setup-android@v3

      - name: Install Android SDK packages
        if: matrix.language == 'java-kotlin'
        run: sdkmanager 'platforms;android-36' 'build-tools;36.0.0'

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: ${{ matrix.language }}
          build-mode: ${{ matrix.build-mode }}

      - name: Build Android code
        if: matrix.language == 'java-kotlin'
        run: |
          yarn build:android:ci
          (
            cd android
            ./gradlew --no-daemon :app:assembleDebug
          )

      - name: Build iOS code
        if: matrix.language == 'swift'
        run: >-
          xcodebuild
          -project "ios/App/Ferry FYI.xcodeproj"
          -scheme "Ferry FYI"
          -sdk iphonesimulator
          -configuration Debug
          -destination 'generic/platform=iOS Simulator'
          ARCH=arm64
          CODE_SIGNING_ALLOWED=NO
          build

      - name: Analyze
        uses: github/codeql-action/analyze@v4
        with:
          category: /language:${{ matrix.language }}
